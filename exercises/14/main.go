package main

/*
## 第 14 课：数据结构

### 题 14.1 用栈判断括号匹配（⭐⭐）

写一个函数 `isValid(s string) bool`，判断字符串中的括号是否匹配。
支持 `()`, `[]`, `{}`。
示例：`"([{}])"` → true，`"([)]"` → false

### 题 14.2 TopK 问题（⭐⭐⭐）

给定一个整数 slice，用 `container/heap` 实现找出最大的 K 个元素。
要求用最小堆，堆大小保持 K。
*/
func main() {}
